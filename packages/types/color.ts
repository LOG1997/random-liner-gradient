export type TRgb = {
    r: number,
    g: number,
    b: number
}
export type THsv = {
    h: number,
    s: number,
    v: number
}
export type THsl = {
    h: number,
    s: number,
    l: number
}
export type THex = string;
